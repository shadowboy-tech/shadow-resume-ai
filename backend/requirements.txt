# Shadow — Backend Dependencies
# Pinned to known-good versions; bump deliberately, not accidentally.

# ── Parsing (Milestone 1) ──
pypdf>=4.0,<5.0            # PDF text extraction (pure Python, no system deps)
python-docx>=1.1,<2.0      # DOCX paragraph extraction

# ── Embedding (Milestone 2) ──
# Offloaded to Mistral API (mistral-embed)

# ── Vector DB (Milestone 2) ──
pinecone>=3.0,<4.0   # Pinecone client (serverless)

# ── API Framework (Milestone 3) ──
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# ── LLM (Milestone 5) ──
mistralai>=2.0,<3.0

# ── Rate Limiting (Milestone 6) ──
slowapi>=0.1,<1.0

# ── Config ──
python-dotenv>=1.0,<2.0    # Load .env files in dev
